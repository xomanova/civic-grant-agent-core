# This is a template - DO NOT commit with real secrets
# Create the actual secret with:
# kubectl create secret generic civic-grant-agent-secrets \
#   --from-literal=google-api-key=YOUR_ACTUAL_KEY
apiVersion: v1
kind: Secret
metadata:
  name: civic-grant-agent-secrets
type: Opaque
stringData:
  google-api-key: "your_google_api_key_here_replace_me"
